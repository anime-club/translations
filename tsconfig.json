{
  "extends": "./node_modules/@10stars/config/tsconfig.json", // only relative paths and local node_modules are supported, see: https://github.com/Microsoft/TypeScript/issues/18865
  "include": ["src/**/*", "types/*.d.ts"]
}
